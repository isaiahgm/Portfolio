/*
SQL database joining
Unspecified combining: Combines tables and gives all possible
Inner join: gives rows that are in both sets
Left/Right join: returns all rows from left/right dataset with matches from other if present
Outer join: returns all ids regardless of match

Match-Merge: in data step
when all values match, SQL inner joins produce the same results
COALESCE function allows you to overlay

Concatenate strings with ||